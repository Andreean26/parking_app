# Application Configuration

# Debug mode - set to "true" to enable logging to STDERR
DEBUG=false

# Future: Database connection string (for extending to persistent storage)
# DB_CONNECTION_STRING=postgres://user:pass@localhost/parking_app

# Future: Default parking lot capacity
# DEFAULT_CAPACITY=100
